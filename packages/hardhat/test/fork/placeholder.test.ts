import { expect } from "chai";
import { ethers } from "hardhat";

describe("Placeholder", () => {
  it("is a placeholder", async () => {
    expect(true).to.equal(true);
    console.log(ethers.utils.formatBytes32String("mint"));
  });
});
